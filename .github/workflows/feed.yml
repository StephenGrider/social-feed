name: deploy-client

on:
  push:
    branches:
      - main
    paths:
      - 'packages/feed/**'

defaults:
  run:
    working-directory: packages/feed

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - run: npm install
      - run: npm run build
